Make sure concurrently is installed

Run npm start from the root folder (this will run nodemon for the server and npm start from the client folder concurrently)

Ensure that you have obtained the Strava client Id and secret and put them both in a .env file.
